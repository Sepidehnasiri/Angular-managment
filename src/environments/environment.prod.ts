export const environment = {
  production: true,
  // Replace this with the final Render backend URL, for example:
  // https://your-render-service-name.onrender.com
  apiBaseUrl: 'REPLACE_WITH_RENDER_BACKEND_URL',
};
